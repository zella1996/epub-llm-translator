# 项目目标

## 当前目标与取舍（2026-09-21 更新）

首要目标是帮助用户读懂并持续阅读自己想读的外文书籍，在阅读原书时锻炼外文思维：
沿原文的信息展开理解主线、插入、限定与语气。英文和日文均已进入实现范围；语言专属能力
与验收边界见 [日文支持与已知限制](docs/japanese-support.md)。
辅助用于解除当前阅读卡点，看懂即可继续原书；中文参考是可用的帮助，不设减少中文
使用的指标。不安排练习、自检题、复述、新句迁移测试或复习任务，也不以“可选”形式保留。
开始阅读不要求先完成校准测试；已有画像可复用，开发侧回归和模型测试照常保留。

用户允许重新评估既有结构，也接受大模型难以精确控制所带来的错误。产品方案首先看
能否读懂并继续读、是否误导或打断阅读，再比较辅助负担和运行成本；不以固定字段、
双阶段、Stanza、专名逐字保留或零语义错误作为不可改变的目标。技术索引、源文件安全、
隐私与预算仍需可靠。

当前调整路线见 [阅读辅助重设计方案](docs/reading-comprehension-redesign-plan.md)；
开始实现时按 [技术改造方案](docs/reading-assistance-implementation-plan.md) 的 P0–P4 执行，
其中明确了内容分工、离线译文覆盖、缓存、预算和验收，不把 Fable 的弹窗建议直接当成实现要求。
[项目说明](PROJECT.md) 记录当前实现、运行基线、有效进度和下一步。
下面保留早期目标供追溯，若与本节及新方案冲突，以最新用户方向为准，不能据此恢复旧约束。

## 长期目标

构建一个面向个人使用、本机运行的命令行工具，将无 DRM、可重排版的英文或日文 EPUB 2/3 转换为适合中文母语者学习和阅读的 Kindle 友好型 EPUB。

工具逐字保留源文正文及原有视觉结构，在需要模型帮助的正文段落后加入通往独立学习解析页的单向链接：提供忠实、克制且可读的段落译文，再在理解卡中暴露源语言表达逻辑；对“费力”或“阻塞”的句子进一步提供以语感和含义理解为中心的轻量解析。英文与日文分别使用版本化的分句、门控、prompt 和校验身份。

本项目首先服务作者本人。GitHub 主要用于代码托管，不以构建通用商业产品或兼容历史接口为目标。

## 阅读体验

- 原文正文必须逐字保留。除定位 ID 和段末普通 `›` 链接外，不改变正文内容与视觉结构。
- 普通正文段落、列表项和块引用先经过本地难度门控；高置信度简单内容在任何模型调用前整体跳过，其余内容获得章末学习解析。标题、诗歌、代码、表格和图片说明暂不处理。
- 普通段落译文忠实、克制且符合中文基本搭配；原文意群顺序主要在理解卡中展开，不以照搬英文句法牺牲可读性。
- 当前版本不自动识别或强制保留专名、亲属词，也不解析人称代词指向；模型只被要求不得补足原文未表达的关系或指代。
- 原书已有的脚注、尾注、链接、编号和样式必须保持不变。
- 学习解析放在对应章节末尾，正文到解析使用普通单向链接；返回原位置使用 Kindle 阅读历史，不生成解析到原文的反向链接。生成链接不使用 `noteref`、`footnote`、`aside` 或上标结构，目标是在 Kindle 上直接打开完整解析页。
- 原书脚注语义保持不变；项目生成内容明确不使用 Kindle 弹窗。

## 困难句与理解卡

句子内部使用三级难度：

- **顺畅**：只提供所在段落的段译。
- **费力**：多读后可以理解，但结构或表达值得学习；根据辅助密度提供简短线索或完整理解卡。
- **阻塞**：无法可靠理解；始终提供完整理解卡。

难句可由句子结构、词语或熟词僻义、中英文语序差异、省略与指代、隐含逻辑、反讽、双关、语气或修辞触发，但前提是它们妨碍理解。文化背景不单独触发解析，只在影响句意时简短补充。

完整理解卡采用轻量结构：

1. **句意**：一句话说明难句在本段中的结论、转折或作用，不重述段译中已经显而易见的完整译文；
2. **线索**：只说明一到两个真正造成理解困难的逻辑、结构、搭配或指代关系，不按英文词序逐块翻译；
3. **语感**：仅在有价值时说明语气、节奏、强调或作者表达特点；
4. **提示**：仅补充当前输入可明确验证的指代、词义或必要限定。

语法术语默认弱化，优先使用“这部分修饰前面的内容”等直白描述；不需要的语感或提示留空。

难句识别采用两阶段机制：快速模型在生成段落译文时逐句判断，本地规则补充疑似漏选项，高质量模型复核候选并生成理解卡。高质量模型拥有最终裁决权。模型结果必须包含可精确映射回正文的句子索引；工具从正文按索引绑定原句，避免模型复写造成任何文本漂移。

系统保存长期个人阅读画像。首次校准使用约 20–30 个真实句子，每本书可再用约 8–12 个句子校准。辅助密度是软目标，不以固定比例强行选择或删除句子。

## 模型与运行方式

- 提供新的 CLI，不保留当前 Python API、固定模型枚举或旧输出格式的兼容性。
- 第一阶段只支持 OpenAI-compatible API；快速模型与高质量模型可以分别配置，也可以指向同一个模型。
- 书名与作者可从 EPUB metadata 读取；作品成书/首版年代必须显式提供，不自动把电子版 `dc:date` 当成作品年代。这些上下文只用于年代词义与语气，不允许模型根据记忆补写情节或现成译本。
- 供应商差异用不含密钥/endpoint 的版本化预设管理，不与翻译结果混为一个缓存。翻译缓存指纹必须包含供应商预设版本、模型快照、思考模式、提示词版本与书籍上下文；API key 不得进入缓存。
- 模型最多接收当前段落，不默认发送相邻段落或整章。
- 启动前估算 token、费用和时间，并支持 dry-run、费用或 token 上限以及章节范围选择。
- 模型调用前执行 `conservative` 本地难度门控：综合句数、词数、从属关系、复杂标点、结构信号、古旧/疑似低频词和个人画像中的困难样本。只有高置信度简单段落才被跳过；不确定内容必须交给模型。另保留短简单对话 16 词、其他短简单单句 10 词的默认阈值。门控支持 `conservative`、`balanced` 和 `off`，被跳过段落不调用任何模型、不生成解析，并从调用量估算中扣除。
- 正式运行前，用户可指定一个正文段落进行预览。工具打印翻译和解析，并生成仅该段落含学习注释的 `PREVIEW` EPUB；允许换段落重新预览。
- 完整运行支持本地缓存、有限重试、进度保存和断点续跑。错误在有限重试后仍存在时，保存进度并停止等待用户介入；不得跳过失败段落或把错误文本写入 EPUB。
- 输入 EPUB 永不被覆盖。预览、正式输出、缓存和诊断产物分开保存。
- 缓存只保存在本机且不进入 Git。日志不得记录 API key，默认不打印完整书籍正文。

## EPUB 与兼容性要求

首期只支持无 DRM、可重排版的 EPUB 2/3。固定版式、脚本、媒体叠加朗读、加密或 DRM 内容明确拒绝处理。

### 输入校验

- 运行前执行安全预检和 EPUBCheck。
- 容器无法安全读取、关键结构缺失、目标 XHTML 无法严格解析、DRM 或路径逃逸等问题必须阻止运行。
- EPUBCheck warning 只报告，不阻止运行。
- EPUBCheck error 默认阻止运行并建议先用其他工具修复；可通过显式兼容选项处理目标正文仍可严格解析的旧书。
- 工具不负责广泛修复原书，只处理完成转换所必需的问题并记录报告。

### 原脚注与学习解析共存

- 项目生成内容在 EPUB 2/3 中统一使用同文档普通单向链接，不声明任何脚注语义；原书 EPUB 3 语义脚注和 EPUB 2 普通脚注保持不变。
- 生成 ID 和 CSS 使用项目专属命名空间并检测碰撞。
- 不分析原脚注内部文本，不修改原有 ID、链接、编号和样式。
- 对由上标或 `noteref` 指向的普通同文件、跨文件脚注目标建立引用图；即使出版社未标注脚注类名，也不得把目标文本当作正文翻译。
- 学习解析放入独立容器；检测到无法隔离的原脚注结构或 CSS 冲突时停止处理该章节。
- 重复运行不得生成第二套学习注释。

### 输出验收

- 转换前后比较 EPUBCheck 结果；输出不得新增 fatal 或 error，也不得恶化被修改资源的既有问题。
- EPUB 打包、XHTML、EPUB 2 NCX 与 EPUB 3 navigation document 必须符合相应规范。
- 所有新旧链接和脚注必须解析到唯一目标；项目生成内容只建立正文到解析的单向链接，返回使用 Kindle 阅读历史。
- 正文文本、段落结构、图片、基础样式、目录和其他资源必须保持视觉与结构等价。
- 机器检查模型响应结构、原句索引绑定、译文非空、数字的明显丢失、异常注释长度和全书完成状态。

工具的正式产物是 EPUB，不依赖或自动调用 Calibre。重要版本另以固定 Calibre 版本和参数转换为 AZW3，并在 Kindle Paperwhite 12 上抽测短注释、长注释、原书脚注共存、单向直达和系统返回。项目生成内容不得触发脚注弹窗。

## 实施里程碑

1. **单段闭环**：新 CLI、预校验、OpenAI-compatible 调用、单段预览、章末普通单向解析链接和合规 EPUB。
2. **全书闭环**：正文识别、段落直译、三级难度、理解卡、双模型、费用估算、缓存和断点恢复。
3. **兼容性加固**：EPUB 2/3、原书脚注共存、差异校验、自动化测试，以及 Calibre → AZW3 → Paperwhite 12 实机验收。
4. **个人化增强**：个人阅读画像、按书校准，以及未来日文支持所需的轻量语言接口。

## 非目标

- 不破解或处理 DRM。
- 不扩展到英文、日文之外的通用多语言产品。
- 不自动生成 AZW3，也不把 Calibre 作为必需依赖。
- 不为项目生成内容请求或依赖 Kindle 脚注弹窗。
- 不负责修复任意损坏或不规范的 EPUB。
- 不追求兼容当前原型的 API、模型枚举和独立译文参考章节。
- 不把个人书籍、缓存、预览、成品或测试书籍提交到 Git。
