# GLM Flash 调优历史摘要

本路径保留为历史链接入口。精确书籍样本、请求与响应、token 用量、
人工评分及本地 trace 路径已移至 Git 忽略的私有归档，由 `PROJECT_LOCAL.md` 索引。

保留的通用结论：单次 prompt 缩短、增加 reasoning 或叠加规则均不能单独证明
阅读质量改善。应冻结输入与判定标准，只改一个变量，记录失败和用量；
正式运行前重新取得针对文本、模型、endpoint 和预算的授权。
